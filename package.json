{
  "name": "orderlab",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd ./packages/server dev",
    "app": "bun run --cwd ./apps/orderlab dev",
    "cli": "bun --cwd ./apps/cli src/index.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.10"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "p-limit": "^7.3.0"
  }
}